import PlaceholderPanel from "../placeholder-panel";

export default function EmpresasPage() {
  return (
    <PlaceholderPanel
      title="Empresas"
      note="Depende da Fase 1 (DDL de Company) e da Fase 3 (API) do roadmap — ainda não implementadas."
    />
  );
}
