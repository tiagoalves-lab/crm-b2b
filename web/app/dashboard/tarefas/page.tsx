import PlaceholderPanel from "../placeholder-panel";

export default function TarefasPage() {
  return (
    <PlaceholderPanel
      title="Tarefas"
      note="Depende da Fase 1 (DDL de Task) e da Fase 4 (activity/task) do roadmap — ainda não implementadas."
    />
  );
}
