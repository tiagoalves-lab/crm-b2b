import PlaceholderPanel from "../placeholder-panel";

export default function ContatosPage() {
  return (
    <PlaceholderPanel
      title="Contatos"
      note="Depende da Fase 1 (DDL de Contact) e da Fase 3 (API) do roadmap — ainda não implementadas."
    />
  );
}
