import PlaceholderPanel from "../placeholder-panel";

export default function PipelinePage() {
  return (
    <PlaceholderPanel
      title="Pipeline"
      note="Depende da Fase 1 (DDL de Opportunity/Pipeline/Stage) e da Fase 3 (API) do roadmap — ainda não implementadas."
    />
  );
}
