import PlaceholderPanel from "../placeholder-panel";

export default function MembrosPage() {
  return (
    <PlaceholderPanel
      title="Membros do workspace"
      note="Depende da Fase 1 (DDL de Workspace/Membership) e da Fase 2 (fluxo de convite) do roadmap — ainda não implementadas. Login já funciona via Supabase Auth; gestão de papéis ainda não."
    />
  );
}
