import PlaceholderPanel from "../placeholder-panel";

export default function LeadsPage() {
  return (
    <PlaceholderPanel
      title="Leads"
      note="Depende do módulo de score e da promoção pro CRM — ver docs/geracao-qualificacao-leads.md (seções 6 e 9). O staging de import já existe (leads/import_empresas.py), mas ainda não é lido por aqui."
    />
  );
}
