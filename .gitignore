# dependencies
node_modules/

# build output
dist/

# environment
.env
.env.local
.env.*.local

# next.js (web/)
.next/
web/next-env.d.ts
web/tsconfig.tsbuildinfo

# deploy CLIs (config local de link do projeto, não segredo mas não deve
# ser versionado — cada máquina/deploy linka o seu)
.vercel
web/.vercel

# logs
*.log
npm-debug.log*

# IDE / local tooling
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# testing
coverage/

# prisma
prisma/*.db

# leads — staging local do módulo de leads (ver leads/import_empresas.py)
leads/*.duckdb
leads/*.duckdb.wal
leads/*.parquet
leads/*.zip

# integração eGestor — planilhas/exports de trabalho com dado real de
# cliente (CNPJ, nome, endereço, e-mail, telefone), coladas em docs/ pra
# eu ler o contexto, nunca pra versionar (regra do projeto: nenhum dado
# real de empresa/pessoa em commit). Scripts (.txt/.md) continuam
# versionados normalmente — só planilha/export com dado real entra aqui.
docs/*.csv
docs/*.xlsx
